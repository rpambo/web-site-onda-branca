{{define "subject"}} Nova Reclamação Recebida - Onda Branca {{end}}

{{define "body"}}
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body {
        font-family: "Segoe UI", Arial, sans-serif;
        background-color: #f9f9f9;
        margin: 0;
        padding: 0;
        color: #323130;
      }
      .container {
        max-width: 600px;
        margin: 40px auto;
        background: #ffffff;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        border: 1px solid #e1dfdd;
      }
      .header {
        background: #c1121f;
        padding: 20px;
        text-align: center;
        color: #ffffff;
        font-size: 18px;
        font-weight: bold;
      }
      .content {
        padding: 30px;
      }
      h2 {
        color: #c1121f;
        margin-bottom: 20px;
        font-weight: 600;
      }
      .details {
        background: #f8f8f8;
        border: 1px solid #e1dfdd;
        border-radius: 6px;
        padding: 15px;
        margin: 25px 0;
      }
      .footer {
        text-align: center;
        padding: 20px;
        font-size: 12px;
        color: #605e5c;
        background: #f3f2f1;
      }
      pre {
        white-space: pre-wrap;
        word-wrap: break-word;
        font-family: "Segoe UI", Arial, sans-serif;
      }
    </style>
  </head>
  <body>
    <div class="container">

      <!-- Header -->
      <div class="header">
        Nova Reclamação Recebida
      </div>

      <!-- Conteúdo -->
      <div class="content">
        <h2>Detalhes da Reclamação</h2>

        <div class="details">
          <p><b>Cliente:</b> {{.Cliente}}</p>
          <p><b>Email:</b> {{.Email}}</p>
          <p><b>Data/Hora:</b> {{.Data}}</p>
        </div>

        <h2>Mensagem do Cliente</h2>
        <div class="details">
          <pre>{{.Message}}</pre>
        </div>

        <p>
          Por favor, entre em contacto com o cliente o mais breve possível.  
          Esta é uma notificação interna automática.
        </p>
      </div>

      <!-- Rodapé -->
      <div class="footer">
        &copy; {{.Data}} Onda Branca — Notificação Interna de Reclamação
      </div>

    </div>
  </body>
</html>
{{end}}